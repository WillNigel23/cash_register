<h1> Products: </h1>

<ul class="products-list">
  <% if @products.any? %>
    <%= render collection: @products, partial: 'product' %>
  <% else %>
    <p> No products available. </p>
  <% end %>
</ul>
